/**
 * Khaki Child script
 */
( function( $ ) {
	'use strict';
	$( window ).bind( 'scroll', function() {
		var distance = 40;
		if ( $( window ).scrollTop() > distance ) {
			$( '.slider-home' ).addClass( 'slider-home-scrolled' );
		} else {
			$( '.slider-home' ).removeClass( 'slider-home-scrolled' );
		}
	});

let wocommeceButton = document.getElementsByClassName()
console.log(wocommeceButton)
	element.classList.contains(class);
	alert('test')

}( jQuery ) );

